/*
 * Varibles, Data Types, and Typing
 */
let wordData = "Word List";

console.log(wordData);

let subTotal = 100;
let tax = subTotal * 0.07;
let totalWithTax = subTotal + tax

let obj = {
	key1: "value",
	key2: 4,
	boolean: true,
	obj2: {
		obj2key1: "Internal Object Value",
	}
};

console.log(obj.key1);